<?php
echo "hello" ;
?>

git init -- to initialize

git status -- t know the status of files

git add . -- to add all files 

git config --global/local user.name "username"  // optional
git config --global/local user.email "email"    // optional

--- (after setting user.name and user.email)
git commit -m "message"  

and then the 3 command after commit as stated in github


U-- untracked
A -- added
m -- modified

